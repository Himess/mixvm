{
  "name": "mixvm",
  "version": "1.0.0",
  "description": "MixVM - Cross-chain Private USDC",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:arc": "hardhat run scripts/deploy.ts --network arc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "dependencies": {
    "@fhevm/solidity": "^0.10.0",
    "snarkjs": "^0.7.4"
  }
}